import React from "react";
import { Shield3DIcon } from "./Shield3DIcon";

export function SafeScoreIcon({ size = 72 }: { size?: number }) {
  return <Shield3DIcon size={size} />;
}

export default SafeScoreIcon;
