/**
 * ui/tokens.ts — Re-exporta de src/theme/tokens.ts
 *
 * Mantém compatibilidade com todos os imports existentes do tipo:
 *   import { colors, radius, shadow } from "@/ui/tokens"
 *   import { colors, radius, shadow } from "@/ui/tokens"
 *
 * Não edite este arquivo. Edite src/theme/tokens.ts.
 */
export {
  colors,
  gradients,
  radius,
  shadow,
  theme,
  typography,
  spacing,
  contentWidth,
  vw,
} from "@/theme/tokens";
